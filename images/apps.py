"""App configuration for the images app."""
from django.apps import AppConfig


class ImagesConfig(AppConfig):
    """Django app configuration for images."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'images'
