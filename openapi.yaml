openapi: 3.0.4
info:
  title: EyesEdge API
  version: 1.0.0
  description: >
    API documentation for the EyesEdge project. This spec reflects the currently
    implemented Django REST Framework endpoints in `cameras`, `motions`, and
    `images` apps.

servers:
  - url: /api
    description: Base URL prefix configured in the Django router.

tags:
  - name: auth
  - name: cameras
  - name: motions
  - name: images

components:
  securitySchemes:
    TokenAuth:
      type: apiKey
      in: header
      name: Authorization
      description: Token auth using header value format `Token <token>`.

  parameters:
    UuidPath:
      name: uuid
      in: path
      required: true
      description: Resource UUID.
      schema:
        type: string
        format: uuid
    CameraAddressFilter:
      name: address
      in: query
      required: false
      schema:
        type: string
        format: uri
    CameraResolutionFilter:
      name: resolution
      in: query
      required: false
      schema:
        type: string
        enum: ["1280x720", "1920x1080", "3840x2160"]
    CameraFpsFilter:
      name: fps
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
    CameraStatusFilter:
      name: status
      in: query
      required: false
      schema:
        type: string
        enum: [active, inactive]
    MotionUuidFilter:
      name: uuid
      in: query
      required: false
      schema:
        type: string
        format: uuid
    MotionCameraUuidFilter:
      name: camera__uuid
      in: query
      required: false
      schema:
        type: string
        format: uuid
    MotionDurationFilter:
      name: duration
      in: query
      required: false
      schema:
        type: number
        minimum: 0
    MotionThresholdFilter:
      name: threshold
      in: query
      required: false
      schema:
        type: number
        minimum: 0
        maximum: 1
    MotionTimestampFilter:
      name: timestamp
      in: query
      required: false
      schema:
        type: string
        format: date-time
    MotionCreatedAtFilter:
      name: created_at
      in: query
      required: false
      schema:
        type: string
        format: date-time
    ImageUuidFilter:
      name: uuid
      in: query
      required: false
      schema:
        type: string
        format: uuid
    ImageMotionEventUuidFilter:
      name: motion_event__uuid
      in: query
      required: false
      schema:
        type: string
        format: uuid
    ImageFilepathFilter:
      name: filepath
      in: query
      required: false
      schema:
        type: string
        format: uri
    ImageFilesizeFilter:
      name: filesize
      in: query
      required: false
      schema:
        type: integer
        minimum: 0
    ImageCreatedAtFilter:
      name: created_at
      in: query
      required: false
      schema:
        type: string
        format: date-time

  schemas:
    AuthTokenRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
          format: password
      example:
        username: admin
        password: secret123

    AuthTokenResponse:
      type: object
      properties:
        token:
          type: string
      required: [token]
      example:
        token: 9944b09199c62bcf9418ad846dd0e4bbdfc6ee4b

    DetailError:
      type: object
      properties:
        detail:
          type: string
      example:
        detail: Not authenticated.

    ValidationError:
      type: object
      additionalProperties:
        type: array
        items:
          type: string
      example:
        address:
          - This field is required.

    Camera:
      type: object
      required: [address, resolution]
      additionalProperties: false
      properties:
        uuid:
          type: string
          format: uuid
          readOnly: true
        url:
          type: string
          format: uri
          readOnly: true
        address:
          type: string
          format: uri
        resolution:
          type: string
          enum: ["1280x720", "1920x1080", "3840x2160"]
        fps:
          type: integer
          minimum: 1
        status:
          type: string
          enum: [active, inactive]
      example:
        uuid: 6b1d6586-1f0f-4bb4-9d43-2d2c913e37b8
        url: /api/cameras/6b1d6586-1f0f-4bb4-9d43-2d2c913e37b8/
        address: http://192.168.1.100/stream
        resolution: "1920x1080"
        fps: 25
        status: active

    MotionEvent:
      type: object
      required: [camera, duration]
      additionalProperties: false
      properties:
        uuid:
          type: string
          format: uuid
          readOnly: true
        url:
          type: string
          format: uri
          readOnly: true
        camera:
          type: string
          format: uuid
        timestamp:
          type: string
          format: date-time
          readOnly: true
        duration:
          type: number
          minimum: 0
        threshold:
          type: number
          minimum: 0
          maximum: 1
        created_at:
          type: string
          format: date-time
          readOnly: true
      example:
        uuid: 1c4a245c-4639-4a35-8763-5a81e6454a1c
        url: /api/motions/1c4a245c-4639-4a35-8763-5a81e6454a1c/
        camera: 6b1d6586-1f0f-4bb4-9d43-2d2c913e37b8
        timestamp: "2026-04-12T10:30:00Z"
        duration: 5.5
        threshold: 0.25
        created_at: "2026-04-12T10:30:00Z"

    Image:
      type: object
      required: [motion_event, filepath]
      additionalProperties: false
      properties:
        uuid:
          type: string
          format: uuid
          readOnly: true
        url:
          type: string
          format: uri
          readOnly: true
        camera:
          type: string
          format: uuid
          readOnly: true
          description: Derived from the related motion event.
        motion_event:
          type: string
          format: uuid
        filepath:
          type: string
          format: uri
        filesize:
          type: integer
          nullable: true
          minimum: 0
        created_at:
          type: string
          format: date-time
          readOnly: true
      example:
        uuid: 6d5d1e71-cb53-4ac2-8239-2ce91dc4d5c7
        url: /api/images/6d5d1e71-cb53-4ac2-8239-2ce91dc4d5c7/
        camera: 6b1d6586-1f0f-4bb4-9d43-2d2c913e37b8
        motion_event: 1c4a245c-4639-4a35-8763-5a81e6454a1c
        filepath: http://storage.local/snapshots/105.jpg
        filesize: 102400
        created_at: "2026-04-12T10:30:02Z"

security:
  - TokenAuth: []

paths:
  /token/:
    post:
      tags: [auth]
      summary: Obtain authentication token
      description: Returns DRF auth token used in `Authorization` header.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthTokenRequest'
      responses:
        '200':
          description: Token created/returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokenResponse'
        '400':
          description: Invalid credentials payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /cameras/:
    get:
      tags: [cameras]
      summary: List cameras
      parameters:
        - $ref: '#/components/parameters/CameraAddressFilter'
        - $ref: '#/components/parameters/CameraResolutionFilter'
        - $ref: '#/components/parameters/CameraFpsFilter'
        - $ref: '#/components/parameters/CameraStatusFilter'
      responses:
        '200':
          description: List of camera resources.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Camera'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    post:
      tags: [cameras]
      summary: Create camera
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Camera'
      responses:
        '201':
          description: Camera created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Camera'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /cameras/{uuid}/:
    parameters:
      - $ref: '#/components/parameters/UuidPath'
    get:
      tags: [cameras]
      summary: Retrieve camera
      responses:
        '200':
          description: Camera resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Camera'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Camera not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    put:
      tags: [cameras]
      summary: Update camera
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Camera'
      responses:
        '200':
          description: Camera updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Camera'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Camera not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    patch:
      tags: [cameras]
      summary: Partially update camera
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Camera'
      responses:
        '200':
          description: Camera updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Camera'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Camera not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    delete:
      tags: [cameras]
      summary: Delete camera
      responses:
        '204':
          description: Camera deleted.
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Camera not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /cameras/{uuid}/motions/:
    parameters:
      - $ref: '#/components/parameters/UuidPath'
    get:
      tags: [cameras, motions]
      summary: List motions by camera
      parameters:
        - $ref: '#/components/parameters/MotionUuidFilter'
        - $ref: '#/components/parameters/MotionDurationFilter'
        - $ref: '#/components/parameters/MotionThresholdFilter'
        - $ref: '#/components/parameters/MotionTimestampFilter'
      responses:
        '200':
          description: Motion events for a camera.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MotionEvent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    post:
      tags: [cameras, motions]
      summary: Create motion event for camera
      description: The camera is forced from path parameter `{uuid}`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [duration]
              properties:
                duration:
                  type: number
                  minimum: 0
                threshold:
                  type: number
                  minimum: 0
                  maximum: 1
            example:
              duration: 4.2
              threshold: 0.35
      responses:
        '201':
          description: Motion event created.
          headers:
            Location:
              description: URL of created motion event.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionEvent'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /cameras/{uuid}/images/:
    parameters:
      - $ref: '#/components/parameters/UuidPath'
    get:
      tags: [cameras, images]
      summary: List images by camera
      parameters:
        - $ref: '#/components/parameters/ImageUuidFilter'
        - $ref: '#/components/parameters/ImageFilepathFilter'
        - $ref: '#/components/parameters/ImageFilesizeFilter'
        - $ref: '#/components/parameters/ImageMotionEventUuidFilter'
      responses:
        '200':
          description: Images for selected camera.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Image'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /motions/:
    get:
      tags: [motions]
      summary: List motion events
      parameters:
        - $ref: '#/components/parameters/MotionUuidFilter'
        - $ref: '#/components/parameters/MotionCameraUuidFilter'
        - $ref: '#/components/parameters/MotionDurationFilter'
        - $ref: '#/components/parameters/MotionThresholdFilter'
        - $ref: '#/components/parameters/MotionTimestampFilter'
        - $ref: '#/components/parameters/MotionCreatedAtFilter'
      responses:
        '200':
          description: List of motion events.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MotionEvent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    post:
      tags: [motions]
      summary: Create motion event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MotionEvent'
      responses:
        '201':
          description: Motion event created.
          headers:
            Location:
              description: URL of created motion event.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionEvent'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /motions/{uuid}/:
    parameters:
      - $ref: '#/components/parameters/UuidPath'
    get:
      tags: [motions]
      summary: Retrieve motion event
      responses:
        '200':
          description: Motion event resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionEvent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Motion event not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    put:
      tags: [motions]
      summary: Update motion event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MotionEvent'
      responses:
        '200':
          description: Motion event updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionEvent'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Motion event not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    patch:
      tags: [motions]
      summary: Partially update motion event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MotionEvent'
      responses:
        '200':
          description: Motion event updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionEvent'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Motion event not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    delete:
      tags: [motions]
      summary: Delete motion event
      responses:
        '204':
          description: Motion event deleted.
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Motion event not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /motions/{uuid}/images/:
    parameters:
      - $ref: '#/components/parameters/UuidPath'
    get:
      tags: [motions, images]
      summary: List images by motion event
      parameters:
        - $ref: '#/components/parameters/ImageUuidFilter'
        - $ref: '#/components/parameters/ImageFilepathFilter'
        - $ref: '#/components/parameters/ImageFilesizeFilter'
        - $ref: '#/components/parameters/ImageCreatedAtFilter'
        - $ref: '#/components/parameters/ImageMotionEventUuidFilter'
      responses:
        '200':
          description: Images for selected motion event.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Image'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    post:
      tags: [motions, images]
      summary: Create image under motion event
      description: The `motion_event` value is forced from path parameter `{uuid}`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filepath]
              properties:
                filepath:
                  type: string
                  format: uri
                filesize:
                  type: integer
                  nullable: true
                  minimum: 0
            example:
              filepath: http://storage.local/snapshots/210.jpg
              filesize: 85412
      responses:
        '201':
          description: Image created.
          headers:
            Location:
              description: URL of created image.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /images/:
    get:
      tags: [images]
      summary: List images
      description: Cached for 300 seconds and varied by Authorization header.
      parameters:
        - $ref: '#/components/parameters/ImageUuidFilter'
        - $ref: '#/components/parameters/ImageMotionEventUuidFilter'
      responses:
        '200':
          description: List of image resources.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Image'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    post:
      tags: [images]
      summary: Create image
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Image'
      responses:
        '201':
          description: Image created.
          headers:
            Location:
              description: URL of created image.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'

  /images/{uuid}/:
    parameters:
      - $ref: '#/components/parameters/UuidPath'
    get:
      tags: [images]
      summary: Retrieve image
      responses:
        '200':
          description: Image resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Image not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    put:
      tags: [images]
      summary: Update image
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Image'
      responses:
        '200':
          description: Image updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Image not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    patch:
      tags: [images]
      summary: Partially update image
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Image'
      responses:
        '200':
          description: Image updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '400':
          description: Validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Image not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
    delete:
      tags: [images]
      summary: Delete image
      responses:
        '204':
          description: Image deleted.
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
        '404':
          description: Image not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'